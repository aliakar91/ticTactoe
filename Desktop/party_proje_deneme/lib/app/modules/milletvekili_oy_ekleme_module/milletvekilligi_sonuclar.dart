import 'package:flutter/material.dart';

class MilletvekilligiSonuclar extends StatelessWidget {
  const MilletvekilligiSonuclar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
